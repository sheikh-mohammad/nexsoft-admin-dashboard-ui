# AGENTS.md — nexsoft-admin-dashboard-ui

Greenfield project. No code or build tooling exists yet.

## Requirements

All scope is defined in `docs/requirements.md`:
- Sidebar + dashboard layout
- Charts and analytics cards
- Responsive navigation
- Reusable components
- Dark/light mode toggle

## Conventions

- `.claude/` is Claude Code's own skill tooling — not project code. Do not edit or reference it.
- Choose a framework before writing code (no framework has been selected yet). Standardize on one approach for components, styling, and state.
- No README exists. If helpful, create one after the first meaningful implementation commit.
